version: 2
jobs:
  build:
    docker:
      - image: cypress/base:16.14.0
    steps:
      - checkout
      - run:
          name: e2e tests
          command: |
            npm run cypress run
      - store_artifacts:
          path: /root/project/cypress/videos/